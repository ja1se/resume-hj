# 프로젝트 가이드: 감성 미니멀리즘 포트폴리오 구현 및 최적화

---

## 1. 프로젝트 개요

- **목표**: 무인양품, 냥스베네, 소울블렌드, 와쿠와쿠 4개 프로젝트 및 'About Me' 섹션 구현.
- **디자인 철학**: 감성 미니멀리즘, 정갈한 여백, 필요한 만큼만 움직이는 절제된 애니메이션을 통해 포트폴리오 브랜드 본질에 집중.
- **기술 스택**: React, Tailwind CSS, GSAP, FontAwesome.

---

## 3. 레이아웃 및 성능 원칙

- **GSAP 메모리 최적화 (`gsap.context`)**: 모든 섹션 애니메이션은 `useLayoutEffect` 내부에서 `gsap.context()`를 사용하여 정의됩니다. 이는 컴포넌트 언마운트 시 모든 애니메이션과 ScrollTrigger 인스턴스를 한 번에 정리(revert)하여 메모리 누수를 방지합니다.
- **스타일 병합 유틸리티 (`cn.js`)**: `clsx`와 `tailwind-merge`를 결합한 `cn` 함수를 사용하여, 동적 클래스 할당 시 발생할 수 있는 Tailwind 클래스 충돌 문제를 해결하고 코드 가독성을 높였습니다.
- **모바일 우선(Mobile-First) 반응형**: Tailwind의 모바일 우선 전략을 기반으로 레이아웃을 설계했으며, `useIsMobile` 훅을 사용하여 저사양 모바일 기기에서의 과도한 애니메이션 연산을 선택적으로 제어합니다.
- **Sticky & Absolute 레이아웃**: `ProjectSection`과 같이 시각적 집중도가 높은 영역은 `sticky` 포지셔닝을 활용하여 스크롤 흐름에 따라 텍스트와 이미지가 자연스럽게 교차되도록 설계했습니다.

---

## 4. 구현 규칙 (Core Rules)

- **데이터 기반 아키텍처**: 프로젝트 상세 정보(기술 스택, 이미지 경로 등)는 `constants/projects.js`에서 중앙 관리됩니다. 이를 통해 새로운 프로젝트 추가 시 별도의 컴포넌트 수정 없이 데이터 추가만으로 확장이 가능합니다.
- **컴포넌트의 원자화(Atomization)**: `Button`, `SectionHeader`, `ArchiveCard` 등 반복되는 UI를 독립적인 컴포넌트로 분리하여 일관된 디자인 시스템을 유지합니다.
- **시맨틱 마크업(Semantic HTML)**: `section`, `header`, `footer`, `main`, `h1~h3` 등 의미 있는 HTML 태그를 사용하여 SEO와 웹 접근성을 고려한 구조를 설계합니다.
- **선언적 애니메이션**: 애니메이션의 시작점(start), 종료점(end), 트리거(trigger)를 직관적으로 선언하여 복잡한 타임라인도 쉽게 관리할 수 있도록 구현합니다.

---

## 5. 인터랙션 정리

- **ScrollTrigger 등장 효과**: 섹션 진입 시 `y: 60`, `opacity: 0`, `scale: 0.8` 등의 속성을 활용하여 아래에서 위로 통통 튀어 오르는(back.out) 입체적인 등장 효과를 적용했습니다.
- **Swiper 가로 슬라이더**: `ArchiveSection`은 그리드 대신 Swiper를 도입하여 모바일에서도 쾌적하게 많은 양의 작업물을 가로 스와이프로 탐색할 수 있도록 구현했으며, 호버 시 이미지가 잘리지 않도록 `overflow: visible` 처리를 적용했습니다.
- **마이크로 인터랙션**: 버튼 호버 시의 그림자 변화, 폼 입력 시의 Border 강조, 연락처 카드 아이콘의 회전 등장 등 작지만 세밀한 피드백을 통해 사용자 경험을 향상시켰습니다.
- **커스텀 디자인 토큰**: `violet-300`, `violet-400` 등 브랜드 컬러를 Swiper 페이지네이션 및 각종 강조 요소에 일관되게 적용하여 시각적 아이덴티티를 확립했습니다.


